URL Fetcher:
    Fetches multiple URLs concurrently.
    Implements both Threading and Asyncio versions.
    Compares performance (time taken by each approach).
    Uses requests (for threading) and aiohttp (for asyncio).